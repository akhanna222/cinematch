"""Pure, dependency-free algorithm core for CineMatch (PRD §7 & §14).

These modules contain no I/O, framework, or database coupling so they are
trivially unit-testable and reusable across services.
"""
